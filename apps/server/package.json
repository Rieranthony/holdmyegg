{
  "name": "@out-of-bounds/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --project server"
  },
  "dependencies": {
    "@better-auth/drizzle-adapter": "^1.6.0",
    "@out-of-bounds/db": "workspace:*",
    "@out-of-bounds/map": "workspace:*",
    "@out-of-bounds/netcode": "workspace:*",
    "@out-of-bounds/sim": "workspace:*",
    "better-auth": "^1.6.0",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.12.12",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
